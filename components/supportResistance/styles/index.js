import {
    StyleSheet
} from 'react-native';

const styles = StyleSheet.create({
    redBold: {
        fontSize: 18,
        color: 'red'
    },
    greenBold: {
        fontSize: 18,
        color: 'green'
    }
});

export default styles;